# Problem HL — петля в списке

Заранее задан (определять его не надо) тип структуры
```
struct node_t {
  struct node_t *next;
  int data;
};
```
Вам необходимо написать функцию с заранее заданной сигнатурой
```
// определяет есть ли петля в односвязном списке
// возвращает 0 если нет и 1 если есть
int list_is_a_loop(struct node_t *top);
```
В посылке должна быть только указанная функция, отправлять функцию main не нужно


## Пример 1
| Ввод | Вывод |
| ---- |-------|
| 0 -1 | 0     |

## Пример 2
| Ввод    | Вывод |
| --------|-------|
| 1000 -1 | 0     |

## Пример 3
| Ввод     | Вывод |
| ---------|-------|
| 1000 500 | 1     |

## Тесты
<https://github.com/tilir/c-graduate/tree/master/coursework/contests/hwalg/tests/problem_hl>